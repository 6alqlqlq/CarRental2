@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated && User.IsInRole("Admin"))
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

      <ul class="nav navbar-nav navbar-right">
         <li>
         @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
         </li>
         <li>
         @Html.ActionLink("ControlPanel", "ControlPanel", "Home", null, htmlAttributes: new { @class = "nav-link", title = "Manage" })
        </li>
        <li>
        @Html.ActionLink("My Account", "Index", "Details",null,htmlAttributes: new { @class = "nav-linkn", title = "Manage" })
    
        </li>
    <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
   </ul>
    }
}
else if (Request.IsAuthenticated && User.IsInRole("User"))
{
    using (Html.BeginForm("LogOff", "Auth", new { area = "Account" }, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("My Rents", "Index", "Rents", routeValues: new { area = "MyRents" }, htmlAttributes: new { @class = "nav-link", title = "Manage" })
            </li>
            <li>
                @Html.ActionLink("My Account", "Index", "Details", routeValues: new { area = "Account" }, htmlAttributes: new { @class = "nav-link", title = "Manage" })
            </li>
            <li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()" class="navbar-button">Log off</a></li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink", @class= "nav-link" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink", @class = "nav-link" })</li>
    </ul>
}
